# セキュリティポリシー

[English](SECURITY.md) | [日本語](SECURITY.ja.md)

## 対応する配置

Itogurumaは、単一Windowsユーザーとloopback限定HTTP endpointでの利用を想定しています。MCP endpointをネットワークインターフェースへ公開せず、SQLiteデータベースを信頼できないユーザーと共有しないでください。

## 秘密情報と認証

すべてのMCPリクエストには`ITOGURUMA_AUTH_TOKEN`のBearerトークンが必要です。トークンを表示、コミット、ログ記録、共有文書への記載の対象にしないでください。設定の有無は`itoguruma auth status`で確認し、漏えいが疑われる場合は`itoguruma auth rotate`を実行します。旧トークンは直ちに無効になるため、すべてのクライアントを再起動または再設定してください。

## データと変更依頼

メッセージには機密情報が含まれる場合があり、ACK後もSQLiteに残ります。OSのアクセス制御でデータベースを保護してください。変更依頼のパスは、設定済み共有CRルートの`inbox/<target_project>/`配下にある既存Markdownファイルだけを許可します。検証失敗時に通常メッセージへ自動変換しません。

Agent履歴削除は破壊的操作で、完全一致かつ大文字小文字を区別するAgent IDだけを対象にします。必ずdry-runを確認してから明示承認に基づいて削除してください。全MCP要求と同様に認証され、一つのSQLite Transactionで実行し、件数とCorrelation IDだけを返します。Message本文やpayloadは応答・Logへ出しません。CLIの実削除では5桁の対話確認も必要です。

SQLiteのプロジェクト一覧は配送先を記録します。認証済み送信者が未登録宛先へ送信すると、その宛先IDを使って有効なプロジェクトとInboxが自動登録されるため、プロジェクトの登録有無は認可境界ではありません。MCPには汎用的なプロジェクト変更Toolを設けません。CLIの明示的な変更では、非リダイレクトの実コンソールから暗号学的乱数による5桁コードを60秒以内、最大3回で再入力します。コードと入力値はログへ記録しません。

## 脆弱性の報告

脆弱性はリポジトリ所有者へ非公開で報告してください。公開Issueへトークン、メッセージデータベース、顧客データ、攻撃手順を記載しないでください。
